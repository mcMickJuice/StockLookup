'use strict';

/**
 * @ngdoc function
 * @name stockMarketApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the stockMarketApp
 */
angular.module('stockMarketApp')
  .controller('homeCtrl', function ($scope) {
  });
